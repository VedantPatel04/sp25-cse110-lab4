1. the console will print the last index of prices: value == prices.length
2. the console will print the last discounted price (before rounding) in the prices variable
3. the console will print the final (rounded) discounted price
4. this function will return an array of all discounted prices from the prices input
5. error: i is not accessible outside the scope of the for-loop
6. error: discountedPrice is not accessible outside the for-loop
7. the console will print the final discounted value in prices (variable declared in function-scope and we are accessing it from function scope too so no error)
   
8. an array of discounted prices
9. error: variable i out of scope
10. print the length of the prices variable
11. the array after discounted prices have been pushed (we can mutate the const variable discounted, but we can't reassign it)
    
12. A - student.name
    B - student["Grad Year"]
    C - student.greeting()
    D - student["Favorite Teacher].name
    E - student.courseLoad[0]

13. 
    A. 32
    B. 1
    C. 3
    D. 3null
    E. 4
    F. 0
    G. 3undefined
    H. NaN

14. 
    A. True
    B. False
    C. True
    D. False
    E. False
    F. True

15. The '==' operator checks for equivalence but only of the values, it is a LOOSE equality operator. The '===' compares not only the values but also their types for equivalence - it is a STRICT equality operator.
17.The call to modifyArray will return the array [2,4,6]:

You  create an empty array. For each value in the input array, you loop over them, multiply each value by 2, so 1 becomes 2, 2 becomes 4, and 3 becomes 6 and then you push these multiplied values to the new copy array we created at the beginning.

19. 1
    4
    3
    2